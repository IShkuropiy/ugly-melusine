import React, { useState } from 'react';
import { Link } from 'react-router-dom'; // If using React Router

const Navbar = () => {
  const [showDropdown, setShowDropdown] = useState(false);

  return (
    <nav className="navbar">
      <ul>
        <li>
          <Link to="/">Home</Link>
        </li>
        <li
          onMouseEnter={() => setShowDropdown(true)}
          onMouseLeave={() => setShowDropdown(false)}
        >
          <Link to="/products">Products</Link>
          {showDropdown && (
            <ul className="submenu">
              <li><Link to="/products/category1">Category 1</Link></li>
              <li><Link to="/products/category2">Category 2</Link></li>
            </ul>
          )}
        </li>
        <li>
          <Link to="/about">About</Link>
        </li>
      </ul>
    </nav>
  );
};

export default Navbar;

/* Basic Navbar Styling */
.navbar ul {
  list-style: none;
  display: flex;
}

.navbar li {
  position: relative; /* Important for submenu positioning */
}

/* Submenu Styling */
.submenu {
  list-style: none;
  position: absolute;
  top: 100%; /* Position below the parent link */
  left: 0;
  background-color: #f9f9f9;
  min-width: 160px;
  box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
  z-index: 1;
  display: flex; /* For vertical stacking of submenu items */
  flex-direction: column;
}

.submenu li a {
  padding: 12px 16px;
  text-decoration: none;
  display: block;
  color: black;
}

.submenu li a:hover {
  background-color: #ddd;
}

///////

        <nav className="navbar">
            <ul className="navlist">
                <ul>
                    <li><NavLink to="/" className={({ isActive }) => isActive ? "navLink active" : "navLink"}>Home</NavLink></li>
                    <li onMouseEnter={() => setShowDropdown(true)}
                        onMouseLeave={() => setShowDropdown(false)} ></li>
                </ul>

                <li><NavLink to="/epoques" className={({ isActive }) => isActive ? "navLink active" : "navLink"}>Epoques</NavLink></li>
                <li><NavLink to="/genres" className={({ isActive }) => isActive ? "navLink active" : "navLink"}>Genres</NavLink></li>
                <li><NavLink to="/gallery" className={({ isActive }) => isActive ? "navLink active" : "navLink"}>Gallery</NavLink></li>
                <li><NavLink to="/gallery" className={({ isActive }) => isActive ? "navLink active" : "navLink"}>Children</NavLink></li>

            </ul>
        </nav>

        css 

  .navbar {
  padding: 1rem 0;
  position: relative;
  z-index: 2;
  background-color: #131314;
  color: #C6011F;
}

.navbar .navlist {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  gap: 2rem;
}

.navLink{
  text-decoration: none;
  color: #C6011F;
  font-size: 1.6rem;
  letter-spacing: 0.12rem;
  cursor: pointer;
}

.navLink:hover {
  text-decoration: none;
  color: #fff;
}